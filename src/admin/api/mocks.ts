// Mocks removed — backend is live. Keep a no-op export for compatibility with any leftover imports.
export function enableMocks() {
  // Intentionally no-op. Runtime mocks have been removed.
  return null;
}
